<!DOCTYPE html>

<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1,user-scalable=no">
<title>Girls Night 💅</title>
<link href="https://fonts.googleapis.com/css2?family=Fredoka:wght@400;600;700&family=Nunito:wght@600;700;800&display=swap" rel="stylesheet">
<script src="https://unpkg.com/peerjs@1.5.2/dist/peerjs.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"></script>
<style>
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
:root{
  --pk:#FF88BB;--pk-d:#FF5599;--pk-bg:#FFF0F5;
  --pu:#CC99FF;--pu-d:#AA55FF;--pu-bg:#F8F0FF;
  --bl:#88CCFF;--bl-d:#4499FF;--bl-bg:#F0F8FF;
  --ye:#FFE066;--ye-d:#FFB800;--ye-bg:#FFFBF0;
  --gn:#88EEC0;--gn-d:#22BB88;--gn-bg:#F0FFF8;
  --bg:#FFFAF8;--wh:#fff;--tx:#3D2C4E;--mu:#9B8AAD;
  --sh:0 4px 20px rgba(61,44,78,.1);--sh-lg:0 8px 40px rgba(61,44,78,.15);
  --ff:'Fredoka',sans-serif;--fb:'Nunito',sans-serif;
}
html,body{min-height:100%;font-family:var(--ff);background:var(--bg);color:var(--tx)}
body{min-height:100vh;overflow-x:hidden}
#root{min-height:100vh}

.tv{min-height:100vh;display:flex;flex-direction:column;align-items:center;justify-content:center;padding:24px;position:relative}
.tv::before{content:’’;position:fixed;inset:0;z-index:0;pointer-events:none;
background:radial-gradient(circle at 10% 15%,rgba(255,136,187,.2) 0%,transparent 40%),
radial-gradient(circle at 90% 85%,rgba(204,153,255,.2) 0%,transparent 40%),
radial-gradient(circle at 80% 10%,rgba(136,204,255,.18) 0%,transparent 36%),
radial-gradient(circle at 20% 90%,rgba(136,238,192,.18) 0%,transparent 36%)}
.tv-w{position:relative;z-index:1;width:100%;max-width:1100px}

.ph{min-height:100vh;display:flex;flex-direction:column;align-items:center;background:var(–bg);padding:18px 14px 32px}
.ph::before{content:’’;position:fixed;inset:0;z-index:0;pointer-events:none;background:radial-gradient(circle at 50% 0%,rgba(255,136,187,.14) 0%,transparent 50%)}
.ph-w{position:relative;z-index:1;width:100%;max-width:420px}

.card{background:rgba(255,255,255,.96);border-radius:22px;padding:28px;box-shadow:var(–sh-lg);border:1.5px solid rgba(61,44,78,.07)}
.card-s{background:rgba(255,255,255,.96);border-radius:18px;padding:18px;box-shadow:var(–sh);border:1.5px solid rgba(61,44,78,.07)}

.t1{font-size:clamp(28px,5vw,52px);font-weight:700;line-height:1.1;color:var(–tx)}
.t2{font-size:clamp(20px,3.2vw,32px);font-weight:700;color:var(–tx)}
.tb{font-family:var(–fb);font-size:16px;color:var(–mu);line-height:1.6}
.tc{text-align:center}
.lbl{font-family:var(–fb);font-size:11px;text-transform:uppercase;letter-spacing:2px;color:var(–mu);font-weight:800;margin-bottom:8px}

.btn{font-family:var(–ff);font-weight:700;font-size:18px;border:none;border-radius:13px;padding:13px 24px;cursor:pointer;transition:all .2s;display:inline-flex;align-items:center;justify-content:center;gap:8px;white-space:nowrap}
.btn:hover:not(:disabled){transform:translateY(-2px);filter:brightness(1.08)}
.btn:active:not(:disabled){transform:translateY(0)}
.btn:disabled{opacity:.35;cursor:not-allowed}
.btn-pk{background:linear-gradient(135deg,#FF88BB,#FF4488);color:#fff;box-shadow:0 4px 16px rgba(255,68,136,.35)}
.btn-pu{background:linear-gradient(135deg,#CC99FF,#AA44FF);color:#fff;box-shadow:0 4px 16px rgba(170,68,255,.35)}
.btn-ye{background:linear-gradient(135deg,#FFE066,#FFB800);color:#3A2800;box-shadow:0 4px 16px rgba(255,184,0,.3)}
.btn-gn{background:linear-gradient(135deg,#88EEC0,#22BB88);color:#002A1A;box-shadow:0 4px 16px rgba(34,187,136,.3)}
.btn-ghost{background:rgba(61,44,78,.06);border:1.5px solid rgba(61,44,78,.15);color:var(–mu)}
.btn-full{width:100%;font-size:19px;padding:16px}

.inp{font-family:var(–ff);font-size:18px;background:rgba(61,44,78,.05);border:2px solid rgba(61,44,78,.12);border-radius:12px;color:var(–tx);padding:13px 16px;width:100%;outline:none;transition:border-color .2s}
.inp:focus{border-color:var(–pk-d);background:var(–pk-bg)}
.inp::placeholder{color:rgba(61,44,78,.3)}
.txta{font-family:var(–ff);font-size:18px;background:rgba(61,44,78,.05);border:2px solid rgba(61,44,78,.12);border-radius:12px;color:var(–tx);padding:13px 16px;width:100%;outline:none;transition:border-color .2s;resize:none;min-height:100px;line-height:1.5}
.txta:focus{border-color:var(–pk-d);background:var(–pk-bg)}
.txta::placeholder{color:rgba(61,44,78,.3)}

.qbox{background:linear-gradient(135deg,var(–pk-bg),var(–pu-bg));border:2px solid var(–pk);border-radius:18px;padding:22px 24px;text-align:center;margin-bottom:20px}
.qbox-tag{font-family:var(–fb);font-size:12px;letter-spacing:2.5px;text-transform:uppercase;color:var(–pk-d);font-weight:800;margin-bottom:8px}
.qbox-text{font-size:clamp(20px,3.2vw,34px);font-weight:700;line-height:1.4;color:var(–tx)}

.cgrid{display:grid;grid-template-columns:repeat(5,1fr);gap:8px;margin:12px 0}
@media(max-width:460px){.cgrid{grid-template-columns:repeat(4,1fr)}}
.ccard{border:2.5px solid rgba(61,44,78,.09);border-radius:14px;padding:10px 6px;cursor:pointer;transition:all .2s;background:var(–wh);display:flex;flex-direction:column;align-items:center;gap:5px;position:relative}
.ccard:hover{transform:translateY(-2px);box-shadow:0 4px 14px rgba(61,44,78,.12)}
.ccard.on{border-color:var(–pk-d);background:var(–pk-bg);transform:translateY(-2px);box-shadow:0 4px 16px rgba(255,68,136,.25)}
.ccard.on::after{content:‘✓’;position:absolute;top:5px;right:7px;font-size:11px;font-weight:800;color:var(–pk-d);font-family:var(–fb)}
.ccard.taken{opacity:.4;cursor:not-allowed;pointer-events:none}
.cname{font-size:13px;font-weight:700;color:var(–tx)}
.cart{height:64px;display:flex;align-items:center;justify-content:center}

.ac{background:var(–wh);border:2px solid rgba(61,44,78,.1);border-radius:13px;padding:14px 18px;font-size:clamp(15px,1.8vw,19px);font-family:var(–fb);cursor:pointer;transition:all .2s;display:flex;align-items:center;gap:12px;font-weight:700;color:var(–tx)}
.ac:hover:not(.ac-off){border-color:var(–pk-d);background:var(–pk-bg);transform:translateY(-2px);box-shadow:var(–sh)}
.ac.ac-off{opacity:.3;cursor:default}
.ac.voted{border-color:var(–pu-d);background:var(–pu-bg)}
.ac-n{width:30px;height:30px;border-radius:50%;background:rgba(61,44,78,.08);display:flex;align-items:center;justify-content:center;font-size:14px;font-weight:800;flex-shrink:0}

.pchip{display:flex;align-items:center;gap:10px;padding:9px 14px;background:var(–wh);border-radius:11px;border:2px solid rgba(61,44,78,.08);margin-bottom:7px}
.pchip-n{flex:1;font-size:16px;font-weight:700}

.rv{border-radius:13px;padding:14px 18px;margin-bottom:8px;display:flex;align-items:center;gap:12px;position:relative;overflow:hidden;font-family:var(–fb)}
.rv-bar{position:absolute;left:0;top:0;height:100%;border-radius:13px;opacity:.18;transition:width .9s ease}
.rv-inner{position:relative;z-index:1;display:flex;align-items:center;gap:10px;width:100%}
.rv-txt{flex:1;font-size:clamp(14px,1.8vw,18px);font-weight:700;color:var(–tx)}
.rv-pts{margin-left:auto;font-size:14px;font-weight:800;white-space:nowrap}

.lb{display:flex;align-items:center;gap:11px;padding:10px 14px;border-radius:13px;margin-bottom:5px}
.lb-rk{width:32px;text-align:center;font-size:18px;font-weight:800}
.lb-nm{flex:1;font-size:clamp(16px,2vw,20px);font-weight:700}
.lb-bw{flex:1;max-width:240px;height:9px;background:rgba(61,44,78,.08);border-radius:4px;overflow:hidden}
.lb-bf{height:100%;border-radius:4px;transition:width 1.2s ease}
.lb-sc{font-size:clamp(16px,2vw,20px);font-weight:800;min-width:55px;text-align:right;font-family:var(–fb)}
.lb-ch{width:40px;height:40px;border-radius:10px;display:flex;align-items:center;justify-content:center;overflow:hidden;flex-shrink:0}

.tw{height:8px;background:rgba(61,44,78,.08);border-radius:4px;overflow:hidden;margin-bottom:16px}
.tb2{height:100%;border-radius:4px;background:linear-gradient(90deg,var(–pk-d),var(–pu-d));transition:width 1s linear}

.pill{display:inline-flex;align-items:center;gap:5px;border-radius:100px;padding:6px 14px;font-size:13px;font-weight:700;font-family:var(–fb)}
.pl-pk{background:var(–pk-bg);color:var(–pk-d);border:1.5px solid var(–pk)}
.pl-pu{background:var(–pu-bg);color:var(–pu-d);border:1.5px solid var(–pu)}
.pl-gn{background:var(–gn-bg);color:var(–gn-d);border:1.5px solid var(–gn)}
.pl-ye{background:var(–ye-bg);color:#AA8800;border:1.5px solid var(–ye-d)}

.rc{font-size:clamp(36px,7vw,68px);font-weight:700;letter-spacing:8px;color:var(–pu-d);font-family:var(–fb);line-height:1}

.ph-q{background:linear-gradient(135deg,var(–pk-bg),var(–pu-bg));border:2px solid var(–pk);border-radius:15px;padding:16px 18px;text-align:center;margin-bottom:14px}
.ph-qt{font-family:var(–fb);font-size:11px;letter-spacing:2px;text-transform:uppercase;color:var(–pk-d);font-weight:800;margin-bottom:6px}
.ph-qq{font-size:clamp(15px,4.5vw,20px);font-weight:700;line-height:1.4;color:var(–tx)}

hr{border:none;border-top:1.5px solid rgba(61,44,78,.08);margin:18px 0}
::-webkit-scrollbar{width:4px}::-webkit-scrollbar-thumb{background:rgba(61,44,78,.2);border-radius:4px}

@keyframes fadeUp{from{opacity:0;transform:translateY(16px)}to{opacity:1;transform:translateY(0)}}
@keyframes pop{0%{transform:scale(.8);opacity:0}70%{transform:scale(1.06)}100%{transform:scale(1);opacity:1}}
@keyframes shimmer{0%,100%{opacity:.5}50%{opacity:1}}
@keyframes zoomIn{from{transform:scale(.5) rotate(-8deg);opacity:0}to{transform:scale(1) rotate(0);opacity:1}}
@keyframes drumShake{0%,100%{transform:translateX(0)}25%{transform:translateX(-3px)}75%{transform:translateX(3px)}}
@keyframes spinSlow{from{transform:rotate(0)}to{transform:rotate(360deg)}}
.fu{animation:fadeUp .4s ease forwards}
.popin{animation:pop .38s ease forwards}
.shim{animation:shimmer 1.5s ease-in-out infinite}
.zoom{animation:zoomIn .6s cubic-bezier(.34,1.56,.64,1) forwards}
.shake{animation:drumShake .15s ease-in-out infinite}

.row{display:flex}.col{flex-direction:column}.g8{gap:8px}.g10{gap:10px}.g12{gap:12px}.g16{gap:16px}.g20{gap:20px}.g24{gap:24px}
.wrap{flex-wrap:wrap}.vc{align-items:center}.hc{justify-content:center}.btw{justify-content:space-between}
.mt8{margin-top:8px}.mt12{margin-top:12px}.mt16{margin-top:16px}.mt20{margin-top:20px}.mt24{margin-top:24px}
.mb8{margin-bottom:8px}.mb12{margin-bottom:12px}.mb16{margin-bottom:16px}.mb20{margin-bottom:20px}.mb24{margin-bottom:24px}

.podium{display:flex;align-items:flex-end;justify-content:center;gap:14px;margin:24px 0}
.pod-col{display:flex;flex-direction:column;align-items:center;gap:8px}
.pod-block{border-radius:14px 14px 0 0;width:120px;display:flex;align-items:flex-start;justify-content:center;padding:12px;font-size:36px;font-weight:800;font-family:var(–fb);color:#fff}
.pod-1{height:140px;background:linear-gradient(180deg,#FFD700,#FFAA00)}
.pod-2{height:100px;background:linear-gradient(180deg,#C0C0C0,#909090)}
.pod-3{height:70px;background:linear-gradient(180deg,#CD7F32,#A0561E)}
@media(max-width:520px){.pod-block{width:90px;font-size:28px}.pod-1{height:110px}.pod-2{height:80px}.pod-3{height:55px}}
</style>

</head>
<body>
<div id="root"></div>
<script>
"use strict";

// ================================================================
// CHARACTERS
// ================================================================
const CHARS = {
butterfly:{name:‘Butterfly’,vibe:‘Always glowing up’,col:’#BB55EE’,bg:’#F8F0FF’,svg:`<svg viewBox="0 0 80 80" xmlns="http://www.w3.org/2000/svg"><ellipse cx="19" cy="33" rx="18" ry="14" fill="#CC88FF"/><ellipse cx="61" cy="33" rx="18" ry="14" fill="#CC88FF"/><ellipse cx="17" cy="32" rx="12" ry="9" fill="#EE99FF"/><ellipse cx="63" cy="32" rx="12" ry="9" fill="#EE99FF"/><ellipse cx="21" cy="52" rx="13" ry="10" fill="#CC88FF"/><ellipse cx="59" cy="52" rx="13" ry="10" fill="#CC88FF"/><ellipse cx="20" cy="52" rx="9" ry="7" fill="#EE99FF"/><ellipse cx="60" cy="52" rx="9" ry="7" fill="#EE99FF"/><circle cx="14" cy="29" r="3" fill="#FFD166"/><circle cx="66" cy="29" r="3" fill="#FFD166"/><circle cx="19" cy="48" r="2.5" fill="#FFD166"/><circle cx="61" cy="48" r="2.5" fill="#FFD166"/><ellipse cx="40" cy="44" rx="6" ry="18" fill="#6622AA"/><circle cx="40" cy="24" r="12" fill="#6622AA"/><circle cx="35" cy="22" r="5" fill="white"/><circle cx="45" cy="22" r="5" fill="white"/><circle cx="35" cy="22" r="2.8" fill="#2D0966"/><circle cx="45" cy="22" r="2.8" fill="#2D0966"/><circle cx="36.2" cy="20.8" r="1.2" fill="white"/><circle cx="46.2" cy="20.8" r="1.2" fill="white"/><circle cx="30" cy="26" r="3.5" fill="rgba(255,100,200,.22)"/><circle cx="50" cy="26" r="3.5" fill="rgba(255,100,200,.22)"/><path d="M 35 29 Q 40 33 45 29" fill="none" stroke="#6622AA" stroke-width="1.8" stroke-linecap="round"/><line x1="37" y1="13" x2="31" y2="5" stroke="#6622AA" stroke-width="1.5" stroke-linecap="round"/><line x1="43" y1="13" x2="49" y2="5" stroke="#6622AA" stroke-width="1.5" stroke-linecap="round"/><circle cx="30" cy="4" r="2" fill="#FF88CC"/><circle cx="50" cy="4" r="2" fill="#FF88CC"/></svg>`},
goose:{name:‘Goose’,vibe:‘Chaotic neutral queen’,col:’#CC7700’,bg:’#FFFBF0’,svg:`<svg viewBox="0 0 80 80" xmlns="http://www.w3.org/2000/svg"><ellipse cx="40" cy="58" rx="24" ry="18" fill="#EEE8D5"/><path d="M 35 44 Q 33 31 37 20" fill="none" stroke="#EEE8D5" stroke-width="13" stroke-linecap="round"/><ellipse cx="58" cy="56" rx="11" ry="7" fill="#DDD8C0" transform="rotate(-15 58 56)"/><circle cx="40" cy="20" r="14" fill="#EEE8D5"/><path d="M 49 18 Q 66 14 68 20 Q 66 26 49 23 Z" fill="#FF8800"/><path d="M 49 20 Q 65 17 68 20" fill="none" stroke="#CC5500" stroke-width="1.2"/><circle cx="36" cy="16" r="6.5" fill="white"/><circle cx="36" cy="16" r="3.8" fill="#1A1030"/><circle cx="37.6" cy="14.8" r="1.5" fill="white"/><circle cx="48" cy="15" r="5" fill="white"/><circle cx="48" cy="15" r="2.8" fill="#1A1030"/><circle cx="49.3" cy="13.8" r="1.1" fill="white"/><circle cx="31" cy="22" r="3" fill="rgba(255,110,110,.2)"/><line x1="34" y1="74" x2="34" y2="78" stroke="#FF8800" stroke-width="3" stroke-linecap="round"/><line x1="46" y1="74" x2="46" y2="78" stroke="#FF8800" stroke-width="3" stroke-linecap="round"/><path d="M 30 78 L 38 78 M 34 78 L 30 78 M 34 78 L 38 78" stroke="#FF8800" stroke-width="3" stroke-linecap="round"/><path d="M 42 78 L 50 78" stroke="#FF8800" stroke-width="3" stroke-linecap="round"/></svg>`},
seaturtle:{name:‘Sea Turtle’,vibe:‘Chill & unbothered’,col:’#119966’,bg:’#E8FFF7’,svg:`<svg viewBox="0 0 80 80" xmlns="http://www.w3.org/2000/svg"><ellipse cx="40" cy="55" rx="27" ry="20" fill="#119966"/><ellipse cx="40" cy="53" rx="19" ry="14" fill="#22BB77"/><polygon points="40,42 45,46 45,52 40,55 35,52 35,46" fill="none" stroke="#119966" stroke-width="1.5"/><ellipse cx="40" cy="63" rx="22" ry="11" fill="#44CC99"/><ellipse cx="14" cy="55" rx="9" ry="6" fill="#44CC99" transform="rotate(-20 14 55)"/><ellipse cx="66" cy="55" rx="9" ry="6" fill="#44CC99" transform="rotate(20 66 55)"/><ellipse cx="18" cy="68" rx="8" ry="5" fill="#44CC99" transform="rotate(10 18 68)"/><ellipse cx="62" cy="68" rx="8" ry="5" fill="#44CC99" transform="rotate(-10 62 68)"/><circle cx="40" cy="28" r="18" fill="#44CC99"/><circle cx="29" cy="34" r="5" fill="rgba(255,100,100,.18)"/><circle cx="51" cy="34" r="5" fill="rgba(255,100,100,.18)"/><circle cx="33" cy="24" r="6" fill="white"/><circle cx="47" cy="24" r="6" fill="white"/><circle cx="33" cy="24" r="3.2" fill="#115533"/><circle cx="47" cy="24" r="3.2" fill="#115533"/><circle cx="34.4" cy="22.7" r="1.4" fill="white"/><circle cx="48.4" cy="22.7" r="1.4" fill="white"/><path d="M 33 33 Q 40 41 47 33" fill="none" stroke="#117755" stroke-width="2" stroke-linecap="round"/></svg>`},
bunny:{name:‘Bunny’,vibe:‘Sweet chaos in fur’,col:’#9944CC’,bg:’#F8F0FF’,svg:`<svg viewBox="0 0 80 80" xmlns="http://www.w3.org/2000/svg"><ellipse cx="29" cy="22" rx="9" ry="20" fill="#E0D0FF"/><ellipse cx="51" cy="22" rx="9" ry="20" fill="#E0D0FF"/><ellipse cx="29" cy="22" rx="5" ry="15" fill="#FFAACC"/><ellipse cx="51" cy="22" rx="5" ry="15" fill="#FFAACC"/><circle cx="40" cy="50" r="25" fill="#E0D0FF"/><ellipse cx="40" cy="72" rx="16" ry="8" fill="#E0D0FF"/><circle cx="28" cy="56" r="5.5" fill="rgba(255,100,100,.2)"/><circle cx="52" cy="56" r="5.5" fill="rgba(255,100,100,.2)"/><circle cx="33" cy="46" r="6.5" fill="white"/><circle cx="47" cy="46" r="6.5" fill="white"/><circle cx="33" cy="46" r="3.5" fill="#6633AA"/><circle cx="47" cy="46" r="3.5" fill="#6633AA"/><circle cx="34.4" cy="44.8" r="1.5" fill="white"/><circle cx="48.4" cy="44.8" r="1.5" fill="white"/><ellipse cx="40" cy="56" rx="3.5" ry="2.8" fill="#FF99BB"/><path d="M 40 59 Q 36 64 32 61" fill="none" stroke="#BB88AA" stroke-width="1.7" stroke-linecap="round"/><path d="M 40 59 Q 44 64 48 61" fill="none" stroke="#BB88AA" stroke-width="1.7" stroke-linecap="round"/></svg>`},
unicorn:{name:‘Unicorn’,vibe:‘Main character energy’,col:’#BB33EE’,bg:’#F8F0FF’,svg:`<svg viewBox="0 0 80 80" xmlns="http://www.w3.org/2000/svg"><ellipse cx="62" cy="42" rx="11" ry="20" fill="#DD99FF"/><ellipse cx="65" cy="38" rx="7" ry="14" fill="#FF88CC"/><ellipse cx="68" cy="44" rx="5" ry="11" fill="#FFD166"/><polygon points="22,28 16,12 32,25" fill="#FFCCEE"/><polygon points="23,27 18,15 31,24" fill="#FFAADD"/><circle cx="40" cy="46" r="26" fill="#FFCCEE"/><polygon points="40,12 33,32 47,32" fill="#FFD700"/><polygon points="40,12 33,32 40,30" fill="#DDA800"/><circle cx="30" cy="52" r="6" fill="rgba(255,100,160,.22)"/><circle cx="50" cy="52" r="6" fill="rgba(255,100,160,.22)"/><circle cx="32" cy="42" r="8" fill="white"/><circle cx="50" cy="42" r="8" fill="white"/><circle cx="32" cy="42" r="4.5" fill="#8822CC"/><circle cx="50" cy="42" r="4.5" fill="#8822CC"/><circle cx="33.6" cy="40.4" r="1.8" fill="white"/><circle cx="51.6" cy="40.4" r="1.8" fill="white"/><line x1="25" y1="36" x2="28" y2="33" stroke="#3D2C4E" stroke-width="1.4" stroke-linecap="round"/><line x1="29" y1="34" x2="31" y2="31" stroke="#3D2C4E" stroke-width="1.4" stroke-linecap="round"/><line x1="42" y1="34" x2="44" y2="31" stroke="#3D2C4E" stroke-width="1.4" stroke-linecap="round"/><line x1="46" y1="34.5" x2="48" y2="32" stroke="#3D2C4E" stroke-width="1.4" stroke-linecap="round"/><circle cx="37" cy="50" r="1.6" fill="#F0A0CC"/><circle cx="43" cy="50" r="1.6" fill="#F0A0CC"/><path d="M 31 54 Q 40 63 49 54" fill="none" stroke="#CC66AA" stroke-width="2.4" stroke-linecap="round"/><circle cx="14" cy="32" r="2" fill="#FFD700"/><circle cx="10" cy="24" r="1.5" fill="#FF88CC"/></svg>`},
worm:{name:‘Worm’,vibe:‘Living her best life’,col:’#FF6BAA’,bg:’#FFF0F8’,svg:`<svg viewBox="0 0 80 80" xmlns="http://www.w3.org/2000/svg"><path d="M 14 56 Q 14 68 24 68 Q 34 68 34 56 Q 34 44 44 44 Q 54 44 54 56 Q 54 68 64 68 Q 70 68 70 60" fill="none" stroke="#FF99CC" stroke-width="14" stroke-linecap="round"/><path d="M 14 56 Q 14 68 24 68 Q 34 68 34 56 Q 34 44 44 44 Q 54 44 54 56" fill="none" stroke="#FFAADD" stroke-width="9" stroke-linecap="round"/><circle cx="14" cy="56" r="14" fill="#FF99CC"/><circle cx="14" cy="56" r="11" fill="#FFAADD"/><circle cx="9" cy="58" r="5" fill="rgba(255,80,140,.2)"/><circle cx="11" cy="52" r="4" fill="white"/><circle cx="17" cy="52" r="4" fill="white"/><circle cx="11" cy="52" r="2.2" fill="#2D0855"/><circle cx="17" cy="52" r="2.2" fill="#2D0855"/><circle cx="11.7" cy="51.2" r="1" fill="white"/><circle cx="17.7" cy="51.2" r="1" fill="white"/><path d="M 11 60 Q 15 64 19 60" fill="none" stroke="#CC4488" stroke-width="1.6" stroke-linecap="round"/><polygon points="14,42 12,35 16,35" fill="#FF6699"/><polygon points="14,42 13,38 15,38" fill="#FF99CC"/></svg>`},
giraffe:{name:‘Giraffe’,vibe:‘Above it all literally’,col:’#CC9933’,bg:’#FFFBF0’,svg:`<svg viewBox="0 0 80 80" xmlns="http://www.w3.org/2000/svg"><ellipse cx="40" cy="68" rx="20" ry="10" fill="#FFCC66"/><rect x="32" y="42" width="16" height="28" rx="8" fill="#FFCC66"/><circle cx="36" cy="50" r="3" fill="#CC8822"/><circle cx="44" cy="58" r="2.5" fill="#CC8822"/><circle cx="38" cy="64" r="2" fill="#CC8822"/><circle cx="40" cy="28" r="14" fill="#FFCC66"/><ellipse cx="40" cy="36" rx="11" ry="7" fill="#FFD988"/><circle cx="26" cy="22" r="4" fill="#FFCC66"/><circle cx="54" cy="22" r="4" fill="#FFCC66"/><line x1="26" y1="18" x2="26" y2="14" stroke="#CC8822" stroke-width="2" stroke-linecap="round"/><line x1="54" y1="18" x2="54" y2="14" stroke="#CC8822" stroke-width="2" stroke-linecap="round"/><circle cx="26" cy="13" r="2.5" fill="#CC8822"/><circle cx="54" cy="13" r="2.5" fill="#CC8822"/><circle cx="29" cy="32" r="4" fill="rgba(255,100,100,.18)"/><circle cx="51" cy="32" r="4" fill="rgba(255,100,100,.18)"/><circle cx="34" cy="25" r="5" fill="white"/><circle cx="46" cy="25" r="5" fill="white"/><circle cx="34" cy="25" r="2.8" fill="#5D3010"/><circle cx="46" cy="25" r="2.8" fill="#5D3010"/><circle cx="35.2" cy="23.8" r="1.2" fill="white"/><circle cx="47.2" cy="23.8" r="1.2" fill="white"/><circle cx="38" cy="35" r="1.5" fill="#CC8822"/><circle cx="42" cy="35" r="1.5" fill="#CC8822"/><path d="M 34 38 Q 40 42 46 38" fill="none" stroke="#AA6611" stroke-width="1.7" stroke-linecap="round"/></svg>`},
dolphin:{name:‘Dolphin’,vibe:‘The smart one’,col:’#3399DD’,bg:’#F0F8FF’,svg:`<svg viewBox="0 0 80 80" xmlns="http://www.w3.org/2000/svg"><path d="M 8 50 Q 20 30 50 32 Q 70 34 72 50 Q 70 66 50 64 Q 30 64 18 60 Q 12 58 8 50 Z" fill="#88CCEE"/><ellipse cx="48" cy="48" rx="22" ry="14" fill="#AADDFF"/><path d="M 4 44 Q -2 36 8 38" fill="none" stroke="#88CCEE" stroke-width="6" stroke-linecap="round"/><path d="M 4 56 Q -2 64 8 62" fill="none" stroke="#88CCEE" stroke-width="6" stroke-linecap="round"/><path d="M 36 28 Q 36 18 44 18 Q 50 22 46 30" fill="#88CCEE"/><path d="M 8 50 Q 4 50 0 56 L 8 54 Z" fill="#88CCEE"/><circle cx="55" cy="42" r="6" fill="white"/><circle cx="55" cy="42" r="3.5" fill="#1A2A55"/><circle cx="56.5" cy="40.7" r="1.4" fill="white"/><circle cx="48" cy="52" r="4.5" fill="rgba(255,100,140,.22)"/><path d="M 60 50 Q 65 56 70 52" fill="none" stroke="#3377AA" stroke-width="2" stroke-linecap="round"/><circle cx="63" cy="46" r="1.5" fill="#3377AA"/></svg>`},
cat:{name:‘Cat’,vibe:‘Too cute too sassy’,col:’#9966DD’,bg:’#F8F0FF’,svg:`<svg viewBox="0 0 80 80" xmlns="http://www.w3.org/2000/svg"><ellipse cx="40" cy="70" rx="18" ry="11" fill="#D4B8F0"/><path d="M 58 70 Q 72 66 70 56 Q 68 50 62 54" fill="none" stroke="#D4B8F0" stroke-width="6" stroke-linecap="round"/><circle cx="40" cy="42" r="28" fill="#D4B8F0"/><polygon points="20,22 14,4 30,18" fill="#D4B8F0"/><polygon points="60,22 66,4 50,18" fill="#D4B8F0"/><polygon points="22,20 18,8 28,18" fill="#F4AACC"/><polygon points="58,20 62,8 52,18" fill="#F4AACC"/><circle cx="29" cy="50" r="5" fill="rgba(255,100,140,.22)"/><circle cx="51" cy="50" r="5" fill="rgba(255,100,140,.22)"/><circle cx="32" cy="38" r="7" fill="white"/><circle cx="48" cy="38" r="7" fill="white"/><ellipse cx="32" cy="38" rx="3" ry="5" fill="#44BB88"/><ellipse cx="48" cy="38" rx="3" ry="5" fill="#44BB88"/><ellipse cx="32" cy="38" rx="1.4" ry="3.5" fill="#1A1230"/><ellipse cx="48" cy="38" rx="1.4" ry="3.5" fill="#1A1230"/><circle cx="33.5" cy="36" r="1.4" fill="white"/><circle cx="49.5" cy="36" r="1.4" fill="white"/><polygon points="40,48 37,52 43,52" fill="#FF88AA"/><path d="M 37 52 Q 34 56 30 54" fill="none" stroke="#AA88C0" stroke-width="1.6" stroke-linecap="round"/><path d="M 43 52 Q 46 56 50 54" fill="none" stroke="#AA88C0" stroke-width="1.6" stroke-linecap="round"/><line x1="14" y1="48" x2="28" y2="50" stroke="#AA88C0" stroke-width="1.3" stroke-linecap="round" opacity=".6"/><line x1="14" y1="54" x2="28" y2="54" stroke="#AA88C0" stroke-width="1.3" stroke-linecap="round" opacity=".6"/><line x1="52" y1="50" x2="66" y2="48" stroke="#AA88C0" stroke-width="1.3" stroke-linecap="round" opacity=".6"/><line x1="52" y1="54" x2="66" y2="54" stroke="#AA88C0" stroke-width="1.3" stroke-linecap="round" opacity=".6"/></svg>`},
frog:{name:‘Frog’,vibe:‘Slay and hop bestie’,col:’#22AA22’,bg:’#F0FFF0’,svg:`<svg viewBox="0 0 80 80" xmlns="http://www.w3.org/2000/svg"><circle cx="40" cy="46" r="30" fill="#5CC05C"/><ellipse cx="40" cy="54" rx="22" ry="17" fill="#88DD88"/><circle cx="26" cy="22" r="13" fill="#5CC05C"/><circle cx="54" cy="22" r="13" fill="#5CC05C"/><circle cx="26" cy="22" r="9" fill="white"/><circle cx="54" cy="22" r="9" fill="white"/><circle cx="26" cy="22" r="5" fill="#1A4422"/><circle cx="54" cy="22" r="5" fill="#1A4422"/><circle cx="27.5" cy="20.5" r="2" fill="white"/><circle cx="55.5" cy="20.5" r="2" fill="white"/><circle cx="27" cy="50" r="5" fill="rgba(255,100,100,.2)"/><circle cx="53" cy="50" r="5" fill="rgba(255,100,100,.2)"/><circle cx="36" cy="45" r="1.8" fill="#3A8A3A"/><circle cx="44" cy="45" r="1.8" fill="#3A8A3A"/><path d="M 22 54 Q 40 72 58 54" fill="#3A8A3A"/><path d="M 24 54 Q 40 70 56 54" fill="#FF88AA"/><ellipse cx="40" cy="65" rx="9" ry="4" fill="#FF5588"/><path d="M 12 50 Q 6 44 10 38" fill="none" stroke="#5CC05C" stroke-width="6" stroke-linecap="round"/><path d="M 68 50 Q 74 44 70 38" fill="none" stroke="#5CC05C" stroke-width="6" stroke-linecap="round"/></svg>`}
};
const CHAR_IDS = Object.keys(CHARS);

// ================================================================
// QUESTIONS
// ================================================================
const Q_FINISH = [
“If I had a warning label, it would say ___”,
“My villain era started when ___”,
“If this chapter of my life had a title, it would be ___”,
“I cannot be trusted around ___”,
“My biggest flex right now is ___”,
“The most ‘me’ thing I’ve ever said is ___”,
“You know it’s a bad day when ___”,
“If someone wrote my dating profile, it would say ___”,
“My Roman Empire is ___”,
“The audacity of me to think ___”,
“My most recent unhinged purchase was ___”,
“If I could text my past self one thing, it would be ___”,
“My love life is basically ___”,
“The last thing I Googled at 2am was ___”,
“Hot take: ___ is actually better than people think”,
“My go-to excuse to get out of plans is ___”,
“I’d never admit this but I actually love ___”,
“The thing that lives in my head rent free is ___”,
“I’m convinced I was a ___ in a past life”,
“My energy today is giving ___”,
];
const Q_HOTTAKE = [
“Pineapne on pizza — die on this hill”,
“What’s your most toxic trait that you’re oddly proud of?”,
“Brunch — overrated or essential?”,
“Best ice cream flavor (if you say vanilla we’re fighting)”,
“Is a hot dog a sandwich? Defend yourself”,
“Most overrated movie of all time”,
“Texting or calling — pick a side”,
“What’s a red flag you actually find attractive?”,
“What’s one thing everyone loves that you secretly hate?”,
“Best decade for fashion and why”,
“What’s the worst snack to bring to a party?”,
“Your hottest opinion about dating apps”,
“Which celebrity gets way too much credit?”,
“What’s a food combo that sounds gross but actually slaps?”,
];
const Q_LIE = [
“I’ve ___ before”,
“I once ___”,
“I can ___”,
“My weirdest skill is that I can ___”,
“Something I’ve never done is ___”,
];
const Q_VIBE = [
“{name} walks into a room like ___”,
“If {name} was a snack she’d be ___”,
“{name} is the type of girl who ___”,
“If {name} was a song she’d be ___”,
“{name} has the same energy as ___”,
“In another life {name} was definitely a ___”,
“{name} would survive a horror movie by ___”,
“If {name} was a reality show she’d be on ___”,
“{name} texts back like ___”,
“{name}‘s aesthetic is giving ___”,
“If {name} was a season she’d be ___”,
“{name}‘s villain origin would be ___”,
];
const Q_DATING = [
“{name} is a 10 but she ___”,
“{name} comes with a warning label that says ___”,
“Swipe right on {name} if you can handle a girl who ___”,
“{name}‘s dating profile says: ‘Looking for someone to ___’”,
“{name}‘s ideal first date is ___”,
“{name}‘s love language is ___”,
“Don’t date {name} unless you’re cool with ___”,
“{name}‘s ick is ___”,
];
const Q_REDFLAG = [
“What’s a tiny red flag of yours?”,
“What’s something you do that you know is unhinged?”,
“Confess your weirdest texting habit”,
“What’s your most embarrassing toxic trait?”,
“What habit of yours would scare a new partner?”,
];
const Q_CAPTION = [
“You show up to the party and your ex is there with someone who looks just like you. Your caption:”,
“You accidentally send the wrong text to your family group chat. It says:”,
“Your alarm doesn’t go off and you’re 2 hours late to a meeting. Your text to your boss:”,
“You spot your celebrity crush in line at Target. What do you do?”,
“Your Uber driver asks ‘rough night?’ What do you say?”,
“It’s 3am and you just texted your ex. The text is:”,
“You’re on a first date and your card declines. Your move:”,
“You see your ex’s new partner in the wild. Your group chat update:”,
];

// ================================================================
// SOUND
// ================================================================
let AC = null;
let MUSIC_ON = true;
let musicNodes = [];

function ac() {
if (!AC) AC = new (window.AudioContext || window.webkitAudioContext)();
if (AC.state === ‘suspended’) AC.resume();
return AC;
}

function note(f, type, start, dur, vol=0.3) {
const c = ac();
const o = c.createOscillator();
const g = c.createGain();
o.connect(g); g.connect(c.destination);
o.type = type; o.frequency.setValueAtTime(f, c.currentTime+start);
g.gain.setValueAtTime(0, c.currentTime+start);
g.gain.linearRampToValueAtTime(vol, c.currentTime+start+0.02);
g.gain.exponentialRampToValueAtTime(0.001, c.currentTime+start+dur);
o.start(c.currentTime+start); o.stop(c.currentTime+start+dur+0.05);
}

const SFX = {
answer: () => { note(880,‘sine’,0,.15,.22); note(1108,‘sine’,.08,.14,.18); },
vote: () => { note(660,‘triangle’,0,.1,.18); note(990,‘triangle’,.05,.1,.15); },
cheer: () => { [523,659,784,1047].forEach((f,i)=>note(f,‘triangle’,i*.06,.25,.2)); },
drum: () => { for(let i=0;i<14;i++) note(110+i*6,‘square’,i*.06,.06,.13); },
fanfare: () => { [523,659,784,1047,1319].forEach((f,i)=>{note(f,‘triangle’,i*.09,.4,.26);note(f*2,‘sine’,i*.09+.04,.2,.1);}); },
ding: () => { note(1568,‘sine’,0,.4,.2); note(2093,‘sine’,.05,.35,.15); },
whoosh: () => { for(let i=0;i<8;i++) note(200-i*15,‘sine’,i*.03,.08,.1); },
pop: () => { note(440,‘square’,0,.06,.15); note(880,‘square’,.04,.05,.12); }
};

function startMusic() {
if (!MUSIC_ON) return;
stopMusic();
const c = ac();
const melody = [523,659,784,659,523,659,784,1047,784,659,523,659];
const beats = melody.length;
const bpm = 90;
const beatDur = 60/bpm * 0.5;
const totalDur = beatDur * beats;

function loop() {
if (!MUSIC_ON) return;
melody.forEach((f,i) => {
const o = c.createOscillator();
const g = c.createGain();
o.connect(g); g.connect(c.destination);
o.type = ‘sine’;
o.frequency.setValueAtTime(f, c.currentTime + i*beatDur);
g.gain.setValueAtTime(0, c.currentTime + i*beatDur);
g.gain.linearRampToValueAtTime(0.04, c.currentTime + i*beatDur + 0.05);
g.gain.exponentialRampToValueAtTime(0.001, c.currentTime + i*beatDur + beatDur*0.9);
o.start(c.currentTime + i*beatDur);
o.stop(c.currentTime + i*beatDur + beatDur);
musicNodes.push(o);
});
setTimeout(loop, totalDur*1000);
}
loop();
}

function stopMusic() {
musicNodes.forEach(n => { try { n.stop(); } catch(e){} });
musicNodes = [];
}

// ================================================================
// UTILS
// ================================================================
const $ = id => document.getElementById(id);
const root = () => $(‘root’);
const shuffle = a => { const b=[…a]; for(let i=b.length-1;i>0;i–){const j=Math.floor(Math.random()*(i+1));[b[i],b[j]]=[b[j],b[i]];} return b; };
const uid = () => Math.random().toString(36).slice(2,8).toUpperCase();
const sanitize = s => String(s||’’).slice(0,200).replace(/[<>]/g,’’);

// ================================================================
// GAME STATE (HOST)
// ================================================================
const ROUND_TYPES = [‘finish’,‘hottake’,‘lie’,‘vibe’,‘dating’,‘redflag’,‘caption’];
const ROUND_LABELS = {
finish: ‘🎀 Finish the Sentence’,
hottake: ‘💅 Hot Take Showdown’,
lie: ‘👀 Lie Detector’,
vibe: ‘🔮 Vibe Assign’,
dating: ‘💌 Dating Profile’,
redflag: ‘🚩 Red Flag Roulette’,
caption: ‘🎬 Caption This’,
};

let HOST = null;
let CLIENT = null;
let MODE = null; // ‘host’ | ‘phone’

// ================================================================
// HOST CLASS
// ================================================================
class HostGame {
constructor() {
this.peer = null;
this.id = uid();
this.players = {};
this.connections = {};
this.scene = ‘lobby’;
this.questionPool = {
finish: […Q_FINISH],
hottake: […Q_HOTTAKE],
lie: […Q_LIE],
vibe: […Q_VIBE],
dating: […Q_DATING],
redflag: […Q_REDFLAG],
caption: […Q_CAPTION],
};
this.roundOrder = this.makeRoundOrder();
this.roundIdx = 0;
this.currentRound = null;
this.roundResponses = {};
this.roundVotes = {};
this.lieRoster = []; // who hasn’t been lie-target yet
this.vibeAssignments = {}; // playerId -> targetId for vibe rounds
this.subjectId = null; // for vibe/dating/lie
this.timer = null;
this.timeLeft = 0;
this.paused = false;
this.revealIdx = 0;
this.shuffledAnswers = [];
}

makeRoundOrder() {
// 16 rounds total, distributed
const order = [];
// act 1: warm up
order.push(‘finish’,‘finish’,‘hottake’,‘caption’,‘finish’);
// act 2: getting personal
order.push(‘lie’,‘redflag’,‘finish’,‘vibe’,‘hottake’);
// act 3: chaos
order.push(‘dating’,‘caption’,‘lie’,‘finish’);
// act 4
order.push(‘hottake’,‘finish’);
return order;
}

init() {
this.peer = new Peer(this.id, {
config: { iceServers: [
{ urls: ‘stun:stun.l.google.com:19302’ },
{ urls: ‘stun:stun1.l.google.com:19302’ }
]}
});
this.peer.on(‘open’, () => { console.log(‘Host ready:’, this.id); render(); });
this.peer.on(‘connection’, (conn) => this.handleConnection(conn));
this.peer.on(‘error’, (err) => { console.error(‘Peer error:’, err); });
}

handleConnection(conn) {
conn.on(‘open’, () => {
console.log(‘Client connected:’, conn.peer);
this.connections[conn.peer] = conn;
conn.send({ type: ‘state’, scene: this.scene, players: this.publicPlayers() });
});
conn.on(‘data’, (data) => this.handleClientMessage(conn, data));
conn.on(‘close’, () => {
delete this.connections[conn.peer];
if (this.players[conn.peer]) {
this.players[conn.peer].connected = false;
}
render();
});
}

publicPlayers() {
return Object.fromEntries(Object.entries(this.players).map(([id,p]) => [id, {
name: p.name, character: p.character, score: p.score, connected: p.connected
}]));
}

handleClientMessage(conn, data) {
const pid = conn.peer;
if (!data || !data.type) return;

```
if (data.type === 'join') {
  const name = sanitize(data.name);
  const character = data.character;
  if (!name || !character || !CHARS[character]) {
    conn.send({ type: 'joinError', reason: 'Invalid name or character' });
    return;
  }
  // check character not taken
  const taken = Object.values(this.players).some(p => p.character === character && p.connected);
  if (taken) {
    conn.send({ type: 'joinError', reason: 'That character is taken!' });
    return;
  }
  this.players[pid] = {
    id: pid, name, character, score: 0, connected: true
  };
  conn.send({ type: 'joined', playerId: pid });
  this.broadcastPlayers();
  render();
}

if (data.type === 'answer') {
  if (this.scene !== 'answer' || !this.currentRound) return;
  this.roundResponses[pid] = sanitize(data.answer);
  conn.send({ type: 'answerReceived' });
  this.broadcastAnswerStatus();
  // check if all answered
  const expected = this.expectedAnswerers();
  const allAnswered = expected.every(id => this.roundResponses[id] !== undefined);
  if (allAnswered) {
    this.endAnswerPhase();
  }
  render();
}

if (data.type === 'vote') {
  if (this.scene !== 'vote') return;
  this.roundVotes[pid] = data.voteFor;
  conn.send({ type: 'voteReceived' });
  const allVoted = this.expectedVoters().every(id => this.roundVotes[id] !== undefined);
  if (allVoted) {
    this.endVotePhase();
  }
  render();
}
```

}

broadcastPlayers() {
const msg = { type: ‘players’, players: this.publicPlayers() };
Object.values(this.connections).forEach(c => { try { c.send(msg); } catch(e){} });
}

broadcastAnswerStatus() {
Object.entries(this.connections).forEach(([pid, c]) => {
try { c.send({ type: ‘answerStatus’, answered: !!this.roundResponses[pid] }); } catch(e){}
});
}

send(pid, msg) {
const c = this.connections[pid];
if (c) { try { c.send(msg); } catch(e){} }
}

broadcast(msg) {
Object.values(this.connections).forEach(c => { try { c.send(msg); } catch(e){} });
}

startGame() {
if (Object.keys(this.players).length < 2) return;
this.roundIdx = 0;
this.lieRoster = shuffle(Object.keys(this.players));
Object.values(this.players).forEach(p => p.score = 0);
startMusic();
this.startRound();
}

expectedAnswerers() {
if (!this.currentRound) return [];
const playerIds = Object.keys(this.players).filter(id => this.players[id].connected);
if (this.currentRound.type === ‘lie’) {
return [this.subjectId];
}
if (this.currentRound.type === ‘vibe’ || this.currentRound.type === ‘dating’) {
return playerIds.filter(id => id !== this.subjectId);
}
return playerIds;
}

expectedVoters() {
return Object.keys(this.players).filter(id => this.players[id].connected);
}

startRound() {
if (this.roundIdx >= this.roundOrder.length) {
this.endGame();
return;
}
const type = this.roundOrder[this.roundIdx];
this.roundResponses = {};
this.roundVotes = {};
this.subjectId = null;
this.vibeAssignments = {};

```
if (type === 'lie') {
  // pick subject
  if (this.lieRoster.length === 0) this.lieRoster = shuffle(Object.keys(this.players));
  this.subjectId = this.lieRoster.shift();
  while (!this.players[this.subjectId]?.connected && this.lieRoster.length > 0) {
    this.subjectId = this.lieRoster.shift();
  }
}
if (type === 'vibe' || type === 'dating') {
  // pick subject = random player
  const ids = Object.keys(this.players).filter(id => this.players[id].connected);
  this.subjectId = ids[Math.floor(Math.random()*ids.length)];
}

// pick question
const pool = this.questionPool[type];
if (!pool || pool.length === 0) {
  this.questionPool[type] = [...this[`Q_${type.toUpperCase()}`] || []];
}
const q = pool[Math.floor(Math.random()*pool.length)];
let questionText = q;
if (this.subjectId && this.players[this.subjectId]) {
  questionText = q.replace(/\{name\}/g, this.players[this.subjectId].name);
}

this.currentRound = {
  type,
  label: ROUND_LABELS[type],
  question: questionText,
  questionTemplate: q,
  idx: this.roundIdx,
};

this.scene = 'answer';
this.timeLeft = (type === 'lie') ? 90 : 60;
this.startTimer(() => this.endAnswerPhase());

// notify phones
const expected = this.expectedAnswerers();
Object.entries(this.connections).forEach(([pid, c]) => {
  const isAnswerer = expected.includes(pid);
  let phoneQuestion = questionText;
  let phaseLabel = this.currentRound.label;
  if (type === 'lie') {
    if (pid === this.subjectId) {
      phoneQuestion = `You're in the hot seat! Write 3 statements about yourself — 2 TRUE and 1 LIE. Separate with new lines.`;
    } else {
      phoneQuestion = `${this.players[this.subjectId].name} is making 3 statements. Wait for the reveal then vote on which is the lie!`;
    }
  } else if (type === 'vibe' || type === 'dating') {
    if (pid === this.subjectId) {
      phoneQuestion = `Sit back — everyone's writing about YOU this round! 💅`;
    }
  }
  try {
    c.send({
      type: 'roundStart',
      roundType: type,
      label: phaseLabel,
      question: phoneQuestion,
      isAnswerer,
      subjectId: this.subjectId,
      subjectName: this.subjectId ? this.players[this.subjectId].name : null,
      timeLeft: this.timeLeft,
    });
  } catch(e){}
});
SFX.whoosh();
render();
```

}

startTimer(onEnd) {
if (this.timer) clearInterval(this.timer);
this.timer = setInterval(() => {
if (this.paused) return;
this.timeLeft–;
if (this.timeLeft <= 5 && this.timeLeft > 0) {
SFX.pop();
}
if (this.timeLeft <= 0) {
clearInterval(this.timer);
this.timer = null;
onEnd();
}
render();
}, 1000);
}

pauseToggle() {
this.paused = !this.paused;
render();
}

endAnswerPhase() {
if (this.timer) { clearInterval(this.timer); this.timer = null; }
SFX.ding();

```
// Build shuffled answers
const type = this.currentRound.type;
if (type === 'lie') {
  // subject's 3 statements
  const raw = this.roundResponses[this.subjectId] || '';
  const statements = raw.split('\n').map(s => s.trim()).filter(Boolean).slice(0,3);
  while (statements.length < 3) statements.push('(no answer)');
  this.shuffledAnswers = statements.map((s, i) => ({
    playerId: this.subjectId,
    text: s,
    statementIdx: i,
  }));
} else {
  const entries = Object.entries(this.roundResponses)
    .filter(([id, ans]) => ans && ans.length > 0)
    .map(([playerId, text]) => ({ playerId, text }));
  // give participation points
  Object.keys(this.roundResponses).forEach(pid => {
    if (this.players[pid]) this.players[pid].score += 200;
  });
  this.shuffledAnswers = shuffle(entries);
}

if (this.shuffledAnswers.length === 0) {
  // skip to next round
  this.roundIdx++;
  setTimeout(() => this.startRound(), 1500);
  return;
}

this.scene = 'vote';
this.timeLeft = 30;
this.startTimer(() => this.endVotePhase());

Object.entries(this.connections).forEach(([pid, c]) => {
  const canVote = this.expectedVoters().includes(pid);
  // for lie rounds, the subject can't vote (they know)
  const myAnswer = this.shuffledAnswers.findIndex(a => a.playerId === pid);
  let voteOptions;
  if (this.currentRound.type === 'lie') {
    voteOptions = this.shuffledAnswers.map((a, i) => ({ idx: i, text: a.text }));
  } else {
    voteOptions = this.shuffledAnswers.map((a, i) => ({ idx: i, text: a.text, isOwn: a.playerId === pid }));
  }
  try {
    c.send({
      type: 'votePhase',
      roundType: this.currentRound.type,
      question: this.currentRound.question,
      options: voteOptions,
      canVote: canVote && !(this.currentRound.type === 'lie' && pid === this.subjectId),
    });
  } catch(e){}
});
render();
```

}

endVotePhase() {
if (this.timer) { clearInterval(this.timer); this.timer = null; }
SFX.drum();

```
// Tally
const voteCounts = {};
Object.values(this.roundVotes).forEach(idx => {
  voteCounts[idx] = (voteCounts[idx] || 0) + 1;
});

const totalVoters = this.expectedVoters().length - (this.currentRound.type === 'lie' ? 1 : 0);
const playersByAnswer = {};
this.shuffledAnswers.forEach((a, i) => {
  const votes = voteCounts[i] || 0;
  a.votes = votes;
  if (this.currentRound.type !== 'lie') {
    const points = votes * 500 + (votes === totalVoters && votes >= 2 ? 1000 : 0);
    a.points = points;
    if (this.players[a.playerId]) {
      this.players[a.playerId].score += points;
    }
  }
});

// For lie round: who guessed correctly gets points
if (this.currentRound.type === 'lie') {
  // host needs to know which is lie - show all 3, host taps which was the lie? Or first stmt is lie?
  // Simpler: subject can mark which is the lie via a follow-up... but we don't have that flow.
  // For MVP: just show all 3 with vote counts and let people self-judge. Award points based on votes received.
  this.shuffledAnswers.forEach(a => { a.points = (a.votes || 0) * 300; });
}

this.scene = 'reveal';
this.revealIdx = 0;
this.broadcast({ type: 'reveal', shouldShowResults: true });
render();
```

}

nextReveal() {
if (this.revealIdx < this.shuffledAnswers.length) {
this.revealIdx++;
SFX.cheer();
render();
} else {
this.scene = ‘leaderboard’;
SFX.fanfare();
this.broadcast({ type: ‘leaderboard’ });
render();
}
}

nextRound() {
this.roundIdx++;
if (this.roundIdx >= this.roundOrder.length) {
this.endGame();
} else {
this.startRound();
}
}

endGame() {
this.scene = ‘final’;
stopMusic();
SFX.fanfare();
setTimeout(() => SFX.cheer(), 800);
setTimeout(() => SFX.cheer(), 1600);
this.broadcast({ type: ‘final’ });
render();
}

reset() {
if (this.timer) clearInterval(this.timer);
stopMusic();
location.reload();
}
}

// ================================================================
// CLIENT (PHONE) CLASS
// ================================================================
class ClientGame {
constructor(roomCode) {
this.roomCode = roomCode.toUpperCase();
this.peer = null;
this.conn = null;
this.scene = ‘name’;
this.name = ‘’;
this.character = null;
this.takenChars = [];
this.players = {};
this.error = null;
this.currentQuestion = null;
this.isAnswerer = false;
this.answerSent = false;
this.voteSent = false;
this.voteOptions = [];
this.canVote = false;
this.subjectId = null;
this.subjectName = null;
this.timeLeft = 0;
}

init() {
this.peer = new Peer({
config: { iceServers: [
{ urls: ‘stun:stun.l.google.com:19302’ },
{ urls: ‘stun:stun1.l.google.com:19302’ }
]}
});
this.peer.on(‘open’, () => {
console.log(‘Phone peer ready’);
this.conn = this.peer.connect(this.roomCode);
this.conn.on(‘open’, () => { this.scene = ‘pickChar’; render(); });
this.conn.on(‘data’, (data) => this.handleHostMessage(data));
this.conn.on(‘close’, () => { this.error = ‘Connection lost’; render(); });
this.conn.on(‘error’, (e) => { this.error = ‘Connection failed’; render(); });
});
this.peer.on(‘error’, (err) => {
console.error(err);
this.error = err.type === ‘peer-unavailable’ ? ‘Room not found! Check the code.’ : ‘Connection issue’;
render();
});
}

handleHostMessage(data) {
if (!data || !data.type) return;

```
if (data.type === 'players') {
  this.players = data.players;
  this.takenChars = Object.values(data.players)
    .filter(p => p.connected)
    .map(p => p.character);
  render();
}
if (data.type === 'state') {
  this.players = data.players || {};
  this.takenChars = Object.values(this.players).filter(p=>p.connected).map(p=>p.character);
  render();
}
if (data.type === 'joined') {
  this.scene = 'waiting';
  render();
}
if (data.type === 'joinError') {
  this.error = data.reason;
  this.scene = 'pickChar';
  render();
}
if (data.type === 'roundStart') {
  this.currentQuestion = data;
  this.isAnswerer = data.isAnswerer;
  this.answerSent = false;
  this.voteSent = false;
  this.timeLeft = data.timeLeft;
  this.scene = 'answer';
  render();
}
if (data.type === 'answerReceived') {
  this.answerSent = true;
  this.scene = 'waiting';
  render();
}
if (data.type === 'votePhase') {
  this.voteOptions = data.options || [];
  this.canVote = data.canVote;
  this.voteSent = false;
  this.scene = 'vote';
  render();
}
if (data.type === 'voteReceived') {
  this.voteSent = true;
  this.scene = 'waiting';
  render();
}
if (data.type === 'reveal') {
  this.scene = 'watching';
  render();
}
if (data.type === 'leaderboard') {
  this.scene = 'watching';
  render();
}
if (data.type === 'final') {
  this.scene = 'finalDrumroll';
  render();
}
```

}

join(name, character) {
this.error = null;
this.name = name;
this.character = character;
this.conn.send({ type: ‘join’, name, character });
}

submitAnswer(answer) {
if (this.answerSent) return;
this.conn.send({ type: ‘answer’, answer });
}

submitVote(idx) {
if (this.voteSent) return;
this.conn.send({ type: ‘vote’, voteFor: idx });
}
}

// ================================================================
// RENDERING
// ================================================================
function render() {
if (MODE === null) {
renderModeSelect();
} else if (MODE === ‘host’) {
renderHost();
} else if (MODE === ‘phone’) {
renderPhone();
}
}

// MODE SELECT
function renderModeSelect() {
root().innerHTML = `
<div class="tv">
<div class="tv-w">
<div class="card tc fu" style="max-width:540px;margin:0 auto">
<div style="font-size:64px;margin-bottom:8px">💅✨</div>
<h1 class="t1 mb12">Girls Night</h1>
<p class="tb mb24">The party game for chaos & connection</p>

```
      <div class="row col g12">
        <button class="btn btn-pk btn-full" id="hostBtn">📺 I'm hosting (TV)</button>
        <button class="btn btn-pu btn-full" id="phoneBtn">📱 I'm joining a game</button>
      </div>

      <hr>
      <p class="tb" style="font-size:13px">
        <strong>Setup:</strong> One person clicks "I'm hosting" on a laptop connected to the TV. Everyone else clicks "I'm joining" on their phones.
      </p>
    </div>
  </div>
</div>
```

`;
$(‘hostBtn’).onclick = () => { MODE = ‘host’; HOST = new HostGame(); HOST.init(); render(); };
$(‘phoneBtn’).onclick = () => { MODE = ‘phone’; render(); };
}

// ================================================================
// HOST RENDER
// ================================================================
function renderHost() {
if (!HOST.peer || !HOST.peer.id) {
root().innerHTML = `<div class="tv"><div class="tv-w"><div class="card tc"><div class="t2 mb16">🎀 Setting up...</div><div class="tb">Connecting to network</div></div></div></div>`;
return;
}

if (HOST.scene === ‘lobby’) return renderHostLobby();
if (HOST.scene === ‘answer’) return renderHostAnswer();
if (HOST.scene === ‘vote’) return renderHostVote();
if (HOST.scene === ‘reveal’) return renderHostReveal();
if (HOST.scene === ‘leaderboard’) return renderHostLeaderboard();
if (HOST.scene === ‘final’) return renderHostFinal();
}

function renderHostLobby() {
const players = Object.values(HOST.players);
const url = `${window.location.origin}${window.location.pathname}?join=${HOST.id}`;
const playerCards = players.map(p => `<div class="pchip popin"> <div class="lb-ch" style="background:${CHARS[p.character].bg};border:2px solid ${CHARS[p.character].col}33;width:36px;height:36px"> <div style="transform:scale(.42);transform-origin:center;width:80px;height:80px;margin:-22px">${CHARS[p.character].svg}</div> </div> <div class="pchip-n" style="color:${CHARS[p.character].col}">${p.name}</div> <div style="font-size:12px;color:var(--gn-d);font-weight:700;font-family:var(--fb)">✓ joined</div> </div>`).join(’’);

root().innerHTML = `
<div class="tv">
<div class="tv-w">
<div class="card fu">
<div class="row g24 wrap" style="align-items:center">
<div style="flex:1;min-width:280px">
<div style="font-size:48px;margin-bottom:8px">💅✨</div>
<h1 class="t1 mb12">Girls Night</h1>
<p class="tb mb20">Scan the QR or open the link on your phone</p>

```
          <div class="card-s" style="background:linear-gradient(135deg,var(--pu-bg),var(--pk-bg));border:2px solid var(--pu);text-align:center;margin-bottom:14px">
            <div class="lbl" style="color:var(--pu-d)">Room Code</div>
            <div class="rc">${HOST.id}</div>
          </div>

          <div id="qrBox" style="background:white;padding:16px;border-radius:14px;display:flex;justify-content:center;align-items:center;border:2.5px dashed var(--pu)"></div>
          <p class="tb mt12" style="font-size:13px;text-align:center"><strong>Or visit:</strong> ${window.location.origin}${window.location.pathname}<br>and enter code <strong>${HOST.id}</strong></p>
        </div>

        <div style="flex:1;min-width:280px">
          <div class="lbl">Players Joined (${players.length})</div>
          <div style="max-height:380px;overflow-y:auto">${playerCards || '<div class="tb tc" style="padding:30px 0">Waiting for players to join...</div>'}</div>

          <button class="btn btn-pk btn-full mt16" id="startGame" ${players.length < 2 ? 'disabled' : ''}>
            ${players.length < 2 ? `Need ${2-players.length} more player${players.length === 1 ? '' : 's'}` : '🔥 Start the Game!'}
          </button>
          <div class="row g8 mt12">
            <button class="btn btn-ghost" id="muteBtn" style="flex:1;font-size:14px">${MUSIC_ON?'🔊 Music On':'🔇 Muted'}</button>
            <button class="btn btn-ghost" id="resetBtn" style="flex:1;font-size:14px">↩ Reset</button>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
```

`;

// QR
setTimeout(() => {
const qr = $(‘qrBox’);
if (qr) {
qr.innerHTML = ‘’;
try {
new QRCode(qr, { text: url, width: 160, height: 160, colorDark: ‘#3D2C4E’, colorLight: ‘#ffffff’ });
} catch(e){
qr.innerHTML = `<div style="padding:20px;font-size:14px;color:var(--mu);text-align:center">${url}</div>`;
}
}
}, 50);

$(‘startGame’).onclick = () => HOST.startGame();
$(‘muteBtn’).onclick = () => { MUSIC_ON = !MUSIC_ON; if(!MUSIC_ON) stopMusic(); else if (HOST.scene !== ‘lobby’) startMusic(); render(); };
$(‘resetBtn’).onclick = () => { if (confirm(‘Reset everything?’)) HOST.reset(); };
}

function renderHostAnswer() {
const r = HOST.currentRound;
const expected = HOST.expectedAnswerers();
const answered = expected.filter(id => HOST.roundResponses[id] !== undefined);
const totalP = expected.length;
const timerPct = (HOST.timeLeft / 60) * 100;

let subjectBlock = ‘’;
if (r.type === ‘lie’ && HOST.subjectId) {
const subj = HOST.players[HOST.subjectId];
subjectBlock = ` <div class="card-s mb16" style="background:var(--ye-bg);border:2px solid var(--ye-d);text-align:center"> <div class="lbl" style="color:#AA8800">In the Hot Seat</div> <div class="row vc hc g12 mt8"> <div class="lb-ch" style="background:${CHARS[subj.character].bg};border:2px solid ${CHARS[subj.character].col}33"> <div style="transform:scale(.5);transform-origin:center;width:80px;height:80px;margin:-20px">${CHARS[subj.character].svg}</div> </div> <div style="font-size:24px;font-weight:700;color:${CHARS[subj.character].col}">${subj.name}</div> </div> </div>`;
}
if ((r.type === ‘vibe’ || r.type === ‘dating’) && HOST.subjectId) {
const subj = HOST.players[HOST.subjectId];
subjectBlock = ` <div class="card-s mb16" style="background:var(--pk-bg);border:2px solid var(--pk);text-align:center"> <div class="lbl" style="color:var(--pk-d)">All Eyes On</div> <div class="row vc hc g12 mt8"> <div class="lb-ch" style="background:${CHARS[subj.character].bg};border:2px solid ${CHARS[subj.character].col}33"> <div style="transform:scale(.5);transform-origin:center;width:80px;height:80px;margin:-20px">${CHARS[subj.character].svg}</div> </div> <div style="font-size:24px;font-weight:700;color:${CHARS[subj.character].col}">${subj.name}</div> </div> </div>`;
}

const playerStatus = expected.map(id => {
const p = HOST.players[id];
if (!p) return ‘’;
const done = HOST.roundResponses[id] !== undefined;
return ` <div class="pchip" style="${done ? 'background:var(--gn-bg);border-color:var(--gn)' : ''}"> <div class="lb-ch" style="background:${CHARS[p.character].bg};border:2px solid ${CHARS[p.character].col}33;width:32px;height:32px"> <div style="transform:scale(.38);transform-origin:center;width:80px;height:80px;margin:-24px">${CHARS[p.character].svg}</div> </div> <div class="pchip-n">${p.name}</div> ${done ? '<div style="color:var(--gn-d);font-size:14px;font-weight:800">✓</div>' : '<div style="color:var(--mu);font-size:13px;font-family:var(--fb)" class="shim">typing...</div>'} </div>`;
}).join(’’);

root().innerHTML = `
<div class="tv">
<div class="tv-w">
<div class="card fu">
<div class="row btw vc mb16">
<div class="rtp" style="background:var(--pk-bg);color:var(--pk-d);border:2px solid var(--pk)">${r.label}</div>
<span class="pill pl-pu">Round ${HOST.roundIdx+1} of ${HOST.roundOrder.length}</span>
</div>

```
      <div class="qbox">
        <div class="qbox-tag">Everyone answers on their phone</div>
        <div class="qbox-text">${r.question}</div>
      </div>

      ${subjectBlock}

      <div class="tw"><div class="tb2" style="width:${timerPct}%"></div></div>

      <div class="row g16 wrap">
        <div style="flex:1;min-width:240px">
          <div class="lbl">Status (${answered.length}/${totalP})</div>
          ${playerStatus}
        </div>
        <div class="card-s" style="background:var(--ye-bg);border:2px solid var(--ye-d);text-align:center;min-width:160px">
          <div class="lbl" style="color:#AA8800">Time Left</div>
          <div class="tnum" style="color:#AA7700;font-size:48px;font-weight:700;font-family:var(--fb);line-height:1">${HOST.timeLeft}</div>
        </div>
      </div>

      <div class="row g8 mt20 hc">
        <button class="btn btn-ghost" id="pauseBtn" style="font-size:14px">${HOST.paused ? '▶ Resume' : '⏸ Pause'}</button>
        <button class="btn btn-pu" id="skipBtn" style="font-size:14px">Skip to vote →</button>
      </div>
    </div>
  </div>
</div>
```

`;
$(‘pauseBtn’).onclick = () => HOST.pauseToggle();
$(‘skipBtn’).onclick = () => HOST.endAnswerPhase();
}

function renderHostVote() {
const r = HOST.currentRound;
const voted = Object.keys(HOST.roundVotes).length;
const totalV = HOST.expectedVoters().length;

const cards = HOST.shuffledAnswers.map((a, i) => `<div class="ac popin" style="cursor:default;animation-delay:${i*.06}s"> <div class="ac-n" style="background:var(--pu-bg);color:var(--pu-d)">${i+1}</div> <div style="flex:1">${a.text}</div> </div>`).join(’’);

root().innerHTML = `
<div class="tv">
<div class="tv-w">
<div class="card fu">
<div class="row btw vc mb16">
<div class="rtp" style="background:var(--pu-bg);color:var(--pu-d);border:2px solid var(--pu)">🗳️ Vote on your phone!</div>
<span class="pill pl-pk">Round ${HOST.roundIdx+1}</span>
</div>

```
      <div class="card-s mb16" style="background:rgba(61,44,78,.04);text-align:center;font-style:italic;font-size:18px;color:var(--mu);font-weight:700;font-family:var(--fb)">
        "${r.question}"
      </div>

      <div class="col g10 mb20">${cards}</div>

      <div class="row btw vc">
        <div class="tb">${voted}/${totalV} have voted</div>
        <div style="font-size:32px;font-weight:700;color:var(--ye-d);font-family:var(--fb)">${HOST.timeLeft}s</div>
      </div>

      <div class="row g8 mt16 hc">
        <button class="btn btn-ghost" id="pauseBtn" style="font-size:14px">${HOST.paused ? '▶ Resume' : '⏸ Pause'}</button>
        <button class="btn btn-pu" id="skipBtn" style="font-size:14px">Show reveal →</button>
      </div>
    </div>
  </div>
</div>
```

`;
$(‘pauseBtn’).onclick = () => HOST.pauseToggle();
$(‘skipBtn’).onclick = () => HOST.endVotePhase();
}

function renderHostReveal() {
const maxVotes = Math.max(1, …HOST.shuffledAnswers.map(a => a.votes || 0));

const reveals = HOST.shuffledAnswers.map((a, i) => {
const revealed = i < HOST.revealIdx;
if (!revealed) {
return ` <div class="rv" style="background:rgba(61,44,78,.05);border:2px solid rgba(61,44,78,.08)"> <div class="rv-inner"> <div class="rv-txt" style="color:rgba(61,44,78,.6)">${a.text}</div> <div class="rv-pts" style="color:var(--mu)">— —</div> </div> </div>`;
}
const p = HOST.players[a.playerId];
if (!p) return ‘’;
const pct = Math.round(((a.votes || 0) / maxVotes) * 100);
const isWinner = a.votes === maxVotes && a.votes > 0;
return `<div class="rv popin" style="background:${p.bg || CHARS[p.character].bg};border:2px solid ${CHARS[p.character].col}44;animation-delay:${(i-Math.max(0,HOST.revealIdx-1))*.1}s"> <div class="rv-bar" style="width:${pct}%;background:${CHARS[p.character].col}"></div> <div class="rv-inner"> <div class="lb-ch" style="background:white;border:2px solid ${CHARS[p.character].col};width:36px;height:36px"> <div style="transform:scale(.4);transform-origin:center;width:80px;height:80px;margin:-24px">${CHARS[p.character].svg}</div> </div> <div style="flex:1"> <div class="rv-txt">${a.text}</div> <div style="font-size:13px;color:${CHARS[p.character].col};font-weight:800;margin-top:3px">${p.name} • ${a.votes||0} vote${a.votes!==1?'s':''}</div> </div> <div class="rv-pts" style="color:${CHARS[p.character].col}"> ${(a.points || 0) > 0 ?`+${a.points}` : '—'} ${isWinner ? '<div style="font-size:11px">🏆 WINS</div>' : ''} </div> </div> </div>`;
}).join(’’);

const allRevealed = HOST.revealIdx >= HOST.shuffledAnswers.length;

root().innerHTML = `
<div class="tv">
<div class="tv-w">
<div class="card fu">
<div class="tc mb16">
<div style="font-size:48px;margin-bottom:8px">👀</div>
<h2 class="t2 mb8">The Reveal!</h2>
<div class="tb" style="font-style:italic">”${HOST.currentRound.question}”</div>
</div>

```
      <div class="col g8 mb20">${reveals}</div>

      <div class="tc">
        ${!allRevealed
          ? `<button class="btn btn-pu btn-full" id="nextRev">Reveal Next (${HOST.revealIdx+1}/${HOST.shuffledAnswers.length}) 👀</button>`
          : `<button class="btn btn-pk btn-full" id="seeBoard">See Leaderboard →</button>`}
      </div>
    </div>
  </div>
</div>
```

`;
if (!allRevealed) {
$(‘nextRev’).onclick = () => HOST.nextReveal();
} else {
$(‘seeBoard’).onclick = () => { HOST.scene = ‘leaderboard’; SFX.fanfare(); HOST.broadcast({type:‘leaderboard’}); render(); };
}
}

function renderHostLeaderboard() {
const sorted = Object.values(HOST.players).sort((a,b) => b.score - a.score);
const max = Math.max(1, …sorted.map(p => p.score));
const isLast = HOST.roundIdx >= HOST.roundOrder.length - 1;

const rows = sorted.map((p, i) => {
const pct = Math.round((p.score / max) * 100);
const isFirst = i === 0;
return ` <div class="lb popin" style="background:${isFirst?CHARS[p.character].bg:'transparent'};border:2px solid ${isFirst?CHARS[p.character].col+'44':'transparent'};animation-delay:${i*.1}s"> <div class="lb-rk" style="color:${isFirst?'#FFAA00':'rgba(61,44,78,.3)'};font-size:${isFirst?22:18}px"> ${i===0?'👑':i+1} </div> <div class="lb-ch" style="background:${CHARS[p.character].bg};border:2px solid ${CHARS[p.character].col}33"> <div style="transform:scale(.45);transform-origin:center;width:80px;height:80px;margin:-22px">${CHARS[p.character].svg}</div> </div> <div class="lb-nm" style="color:${isFirst?CHARS[p.character].col:'var(--tx)'}">${p.name}</div> <div class="lb-bw"><div class="lb-bf" style="width:${pct}%;background:${CHARS[p.character].col}"></div></div> <div class="lb-sc" style="color:${CHARS[p.character].col}">${p.score.toLocaleString()}</div> </div>`;
}).join(’’);

root().innerHTML = `
<div class="tv">
<div class="tv-w">
<div class="card fu">
<div class="tc mb20">
<div style="font-size:52px;margin-bottom:8px">🏆</div>
<h2 class="t2 mb8">Leaderboard</h2>
<div class="tb">After Round ${HOST.roundIdx+1}</div>
</div>

```
      <div class="mb20">${rows}</div>

      <button class="btn btn-pk btn-full" id="nextR">
        ${isLast ? '✨ See Final Results' : 'Next Round →'}
      </button>
    </div>
  </div>
</div>
```

`;
$(‘nextR’).onclick = () => HOST.nextRound();
}

function renderHostFinal() {
const sorted = Object.values(HOST.players).sort((a,b) => b.score - a.score);
const top3 = sorted.slice(0,3);
const rest = sorted.slice(3);

const podiumChar = (p, place) => p ? ` <div class="pod-col"> <div style="background:${CHARS[p.character].bg};border:3px solid ${CHARS[p.character].col};border-radius:20px;padding:14px;width:100px;height:100px;display:flex;align-items:center;justify-content:center"> <div style="transform:scale(.95);transform-origin:center">${CHARS[p.character].svg}</div> </div> <div style="font-size:18px;font-weight:700;color:${CHARS[p.character].col};text-align:center">${p.name}</div> <div style="font-size:14px;font-weight:800;color:var(--ye-d);font-family:var(--fb)">${p.score.toLocaleString()} pts</div> <div class="pod-block pod-${place}">${place}</div> </div>` : `<div class="pod-col"><div style="width:100px;height:100px"></div><div></div><div></div><div class="pod-block pod-${place}">${place}</div></div>`;

const restRows = rest.map((p, i) => {
return ` <div class="lb fu"> <div class="lb-rk">${i+4}</div> <div class="lb-ch" style="background:${CHARS[p.character].bg};border:2px solid ${CHARS[p.character].col}33"> <div style="transform:scale(.45);transform-origin:center;width:80px;height:80px;margin:-22px">${CHARS[p.character].svg}</div> </div> <div class="lb-nm">${p.name}</div> <div class="lb-sc" style="color:${CHARS[p.character].col}">${p.score.toLocaleString()}</div> </div>`;
}).join(’’);

root().innerHTML = `
<div class="tv">
<div class="tv-w">
<div class="card fu">
<div class="tc mb16">
<div style="font-size:64px;margin-bottom:8px">✨🏆✨</div>
<h1 class="t1 mb8">Game Over!</h1>
<div class="tb">Tonight’s results are in…</div>
</div>

```
      <div class="podium">
        ${podiumChar(top3[1], 2)}
        ${podiumChar(top3[0], 1)}
        ${podiumChar(top3[2], 3)}
      </div>

      ${rest.length ? `<hr><div class="lbl tc mb8">Also slaying tonight</div>${restRows}` : ''}

      <hr>
      <div class="row g8 hc">
        <button class="btn btn-pk" id="playAgain">🎀 Play Again (same players)</button>
        <button class="btn btn-ghost" id="newGame">🆕 New Game</button>
      </div>
    </div>
  </div>
</div>
```

`;
$(‘playAgain’).onclick = () => {
Object.values(HOST.players).forEach(p => p.score = 0);
HOST.roundIdx = 0;
HOST.lieRoster = shuffle(Object.keys(HOST.players));
HOST.questionPool = {
finish: […Q_FINISH], hottake: […Q_HOTTAKE], lie: […Q_LIE],
vibe: […Q_VIBE], dating: […Q_DATING], redflag: […Q_REDFLAG], caption: […Q_CAPTION]
};
HOST.roundOrder = HOST.makeRoundOrder();
startMusic();
HOST.startRound();
};
$(‘newGame’).onclick = () => HOST.reset();
}

// ================================================================
// PHONE RENDER
// ================================================================
function renderPhone() {
if (!CLIENT) {
return renderPhoneJoinCode();
}
if (CLIENT.error && CLIENT.scene !== ‘pickChar’) {
return renderPhoneError();
}
if (CLIENT.scene === ‘connecting’) return renderPhoneConnecting();
if (CLIENT.scene === ‘name’) return renderPhoneConnecting();
if (CLIENT.scene === ‘pickChar’) return renderPhonePickChar();
if (CLIENT.scene === ‘waiting’) return renderPhoneWaiting();
if (CLIENT.scene === ‘answer’) return renderPhoneAnswer();
if (CLIENT.scene === ‘vote’) return renderPhoneVote();
if (CLIENT.scene === ‘watching’) return renderPhoneWatching();
if (CLIENT.scene === ‘finalDrumroll’) return renderPhoneFinal();
}

function renderPhoneJoinCode() {
const params = new URLSearchParams(window.location.search);
const autoCode = params.get(‘join’) || ‘’;
root().innerHTML = `<div class="ph"> <div class="ph-w"> <div class="card fu"> <div class="tc mb20"> <div style="font-size:44px;margin-bottom:6px">📱</div> <h2 class="t2 mb8">Join the Game</h2> <p class="tb">Enter the code from the TV</p> </div> <div class="lbl">Room Code</div> <input class="inp tc" id="codeIn" placeholder="ABCDEF" maxlength="6" value="${autoCode}" style="font-size:32px;letter-spacing:8px;text-transform:uppercase;font-family:var(--fb);font-weight:700"> <button class="btn btn-pk btn-full mt16" id="joinBtn">Connect 🎀</button> <button class="btn btn-ghost btn-full mt8" id="backBtn">← Back</button> </div> </div> </div>`;
$(‘codeIn’).focus();
$(‘codeIn’).oninput = (e) => { e.target.value = e.target.value.toUpperCase().replace(/[^A-Z0-9]/g, ‘’); };
$(‘codeIn’).onkeydown = (e) => { if (e.key === ‘Enter’) $(‘joinBtn’).click(); };
$(‘joinBtn’).onclick = () => {
const code = $(‘codeIn’).value.trim().toUpperCase();
if (code.length < 4) return;
CLIENT = new ClientGame(code);
CLIENT.scene = ‘connecting’;
CLIENT.init();
render();
};
$(‘backBtn’).onclick = () => { MODE = null; render(); };
}

function renderPhoneConnecting() {
root().innerHTML = `<div class="ph"> <div class="ph-w"> <div class="card tc fu"> <div style="font-size:48px;margin-bottom:12px" class="shim">⏳</div> <h2 class="t2 mb8">Connecting...</h2> <p class="tb mb16">Joining room <strong>${CLIENT.roomCode}</strong></p> <button class="btn btn-ghost btn-full" id="cancelBtn">Cancel</button> </div> </div> </div>`;
$(‘cancelBtn’).onclick = () => { CLIENT = null; render(); };
}

function renderPhoneError() {
root().innerHTML = `<div class="ph"> <div class="ph-w"> <div class="card tc fu"> <div style="font-size:44px;margin-bottom:8px">😅</div> <h2 class="t2 mb8">Oops!</h2> <p class="tb mb16">${CLIENT.error}</p> <button class="btn btn-pk btn-full" id="retryBtn">Try Again</button> </div> </div> </div>`;
$(‘retryBtn’).onclick = () => { CLIENT = null; render(); };
}

function renderPhonePickChar() {
const charCards = CHAR_IDS.map(id => {
const c = CHARS[id];
const taken = CLIENT.takenChars.includes(id);
const selected = CLIENT.character === id;
return ` <div class="ccard ${selected?'on':''} ${taken?'taken':''}" data-c="${id}"> <div class="cart">${c.svg}</div> <div class="cname">${c.name}</div> </div>`;
}).join(’’);

root().innerHTML = `
<div class="ph">
<div class="ph-w">
<div class="card fu">
<div class="tc mb16">
<div style="font-size:36px">🎀</div>
<h2 class="t2 mb8">Pick Your Character</h2>
<p class="tb" style="font-size:14px">This is you for the whole game</p>
</div>

```
      ${CLIENT.error ? `<div class="card-s mb12" style="background:#FFE5E5;border:2px solid #FF8888;color:#CC2222;font-weight:700;font-size:14px;font-family:var(--fb);text-align:center">${CLIENT.error}</div>` : ''}

      <div class="cgrid">${charCards}</div>

      <div class="lbl mt16">Your Name</div>
      <input class="inp" id="nameIn" placeholder="Enter your name..." maxlength="20" value="${CLIENT.name||''}">

      <button class="btn btn-pk btn-full mt16" id="joinBtn" ${!CLIENT.character?'disabled':''}>
        ${CLIENT.character ? `Let's Go! ✨` : 'Pick a character first'}
      </button>
    </div>
  </div>
</div>
```

`;
document.querySelectorAll(’[data-c]’).forEach(el => {
el.onclick = () => {
const c = el.dataset.c;
if (CLIENT.takenChars.includes(c)) return;
CLIENT.character = c;
CLIENT.error = null;
render();
};
});
$(‘nameIn’).oninput = (e) => { CLIENT.name = e.target.value; };
$(‘joinBtn’).onclick = () => {
const name = $(‘nameIn’).value.trim();
if (!name || !CLIENT.character) return;
CLIENT.join(name, CLIENT.character);
};
}

function renderPhoneWaiting() {
const c = CLIENT.character ? CHARS[CLIENT.character] : null;
root().innerHTML = `<div class="ph"> <div class="ph-w"> <div class="card tc fu"> ${c ?`
<div style="background:${c.bg};border-radius:20px;padding:20px;border:2px solid ${c.col}33;margin-bottom:16px">
${c.svg}
<div style="font-size:22px;font-weight:700;color:${c.col};margin-top:8px">${CLIENT.name}</div>
</div>
`: ''} <div style="font-size:36px;margin-bottom:8px" class="shim">⏳</div> <h2 class="t2 mb8">${CLIENT.answerSent ? 'Answer locked in!' : CLIENT.voteSent ? 'Vote locked in!' : 'Waiting...'}</h2> <p class="tb">${CLIENT.answerSent || CLIENT.voteSent ? 'Watch the TV — results coming up' : 'Wait for the host to start'}</p> </div> </div> </div>`;
}

function renderPhoneAnswer() {
const q = CLIENT.currentQuestion;
const c = CLIENT.character ? CHARS[CLIENT.character] : null;

if (!CLIENT.isAnswerer) {
return root().innerHTML = `<div class="ph"> <div class="ph-w"> <div class="card tc fu"> <div class="ph-q"> <div class="ph-qt">${q.label}</div> <div class="ph-qq">${q.question}</div> </div> <div style="font-size:40px;margin-bottom:8px">👀</div> <p class="tb">Sit tight! You'll get to vote in a minute.</p> </div> </div> </div>`;
}

const isLie = q.roundType === ‘lie’;
root().innerHTML = `<div class="ph"> <div class="ph-w"> <div class="card fu"> ${c ?`<div style="display:flex;align-items:center;gap:8px;margin-bottom:12px">
<div style="width:36px;height:36px;background:${c.bg};border-radius:10px;display:flex;align-items:center;justify-content:center;border:2px solid ${c.col}33;overflow:hidden">
<div style="transform:scale(.45);transform-origin:center;width:80px;height:80px;margin:-22px">${c.svg}</div>
</div>
<div style="font-size:16px;font-weight:700;color:${c.col}">${CLIENT.name}</div>
</div>` : ‘’}

```
      <div class="ph-q">
        <div class="ph-qt">${q.label}</div>
        <div class="ph-qq">${q.question}</div>
      </div>

      <textarea class="txta" id="ansIn" placeholder="${isLie?'2 truths and 1 lie\\n(separate lines)':'Type your answer...'}" maxlength="200"${isLie?' style="min-height:140px"':''}></textarea>

      <button class="btn btn-pk btn-full mt12" id="subBtn">Lock It In ✨</button>
    </div>
  </div>
</div>
```

`;
$(‘ansIn’).focus();
$(‘subBtn’).onclick = () => {
const a = $(‘ansIn’).value.trim();
if (!a) return;
SFX.answer();
CLIENT.submitAnswer(a);
};
}

function renderPhoneVote() {
const c = CLIENT.character ? CHARS[CLIENT.character] : null;

if (!CLIENT.canVote) {
return root().innerHTML = `<div class="ph"> <div class="ph-w"> <div class="card tc fu"> <div style="font-size:40px;margin-bottom:8px">🍿</div> <h2 class="t2 mb8">Watching!</h2> <p class="tb">You can't vote this round — enjoy the show on the TV</p> </div> </div> </div>`;
}

const cards = CLIENT.voteOptions.map((opt, i) => {
const own = opt.isOwn;
return ` <div class="ac ${own?'ac-off':''}" data-vote="${own?'':opt.idx}"> <div class="ac-n" style="background:var(--pu-bg);color:var(--pu-d)">${i+1}</div> <div style="flex:1">${opt.text}</div> ${own ? '<div style="font-size:11px;color:var(--mu);font-family:var(--fb)">yours</div>' : ''} </div>`;
}).join(’’);

root().innerHTML = `<div class="ph"> <div class="ph-w"> <div class="card fu"> ${c ?`<div style="display:flex;align-items:center;gap:8px;margin-bottom:12px">
<div style="width:36px;height:36px;background:${c.bg};border-radius:10px;display:flex;align-items:center;justify-content:center;border:2px solid ${c.col}33;overflow:hidden">
<div style="transform:scale(.45);transform-origin:center;width:80px;height:80px;margin:-22px">${c.svg}</div>
</div>
<div style="font-size:16px;font-weight:700;color:${c.col}">${CLIENT.name}</div>
</div>` : ‘’}

```
      <div class="card-s mb12" style="background:rgba(61,44,78,.04);font-style:italic;font-size:14px;color:var(--mu);font-family:var(--fb);font-weight:700;text-align:center">"${CLIENT.currentQuestion?.question || ''}"</div>

      <div class="lbl tc">Tap the funniest 👇</div>

      <div class="col g8">${cards}</div>
    </div>
  </div>
</div>
```

`;
document.querySelectorAll(’[data-vote]’).forEach(el => {
if (!el.dataset.vote) return;
el.onclick = () => {
SFX.vote();
CLIENT.submitVote(parseInt(el.dataset.vote));
};
});
}

function renderPhoneWatching() {
const c = CLIENT.character ? CHARS[CLIENT.character] : null;
root().innerHTML = `<div class="ph"> <div class="ph-w"> <div class="card tc fu"> ${c ?`<div style="background:${c.bg};border-radius:20px;padding:18px;border:2px solid ${c.col}33;margin-bottom:16px">
${c.svg}
<div style="font-size:20px;font-weight:700;color:${c.col};margin-top:8px">${CLIENT.name}</div>
</div>`: ''} <div style="font-size:36px;margin-bottom:8px">📺</div> <h2 class="t2 mb8">Watch the TV!</h2> <p class="tb">The reveal is happening...</p> </div> </div> </div>`;
}

function renderPhoneFinal() {
root().innerHTML = `<div class="ph"> <div class="ph-w"> <div class="card tc fu"> <div style="font-size:64px;margin-bottom:12px" class="shake">🥁</div> <h2 class="t1 mb12">DRUMROLL...</h2> <p class="tb mb20">The winner is being announced on the TV right now!</p> <div style="font-size:32px;animation:spinSlow 3s linear infinite;display:inline-block">✨</div> </div> </div> </div>`;
}

// ================================================================
// AUTO-START FROM URL
// ================================================================
window.addEventListener(‘load’, () => {
const params = new URLSearchParams(window.location.search);
if (params.get(‘join’)) {
MODE = ‘phone’;
}
render();
});

// Click anywhere to enable audio (browsers require user interaction)
document.addEventListener(‘click’, () => { ac(); }, { once: true });

render();
</script>

</body>
</html>